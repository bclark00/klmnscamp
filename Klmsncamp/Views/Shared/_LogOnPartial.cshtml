@if (Request.IsAuthenticated)
{
    <text><strong>@User.Identity.Name</strong>! [ @Html.ActionLink("Çıkış", "LogOff", "Account")
    ]&nbsp;[ @Html.ActionLink("Şifre Değiştir", "ChangePassword", "Account") ]&nbsp;[@Html.ActionLink("Profil", "Edit", "Account", new { username_ = User.Identity.Name.ToString() },null) ]</text>

}
else
{
    @:[ @Html.ActionLink("Giriş Yap", "LogOn", "Account") ]
}
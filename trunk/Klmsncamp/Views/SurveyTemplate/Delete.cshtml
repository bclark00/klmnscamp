@model Klmsncamp.Models.SurveyTemplate

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>SurveyTemplate</legend>

    <div class="display-label">Description</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Description)
    </div>

    <div class="display-label">RequestType</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.RequestType.Description)
    </div>

    <div class="display-label">PreDefined</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.PreDefined)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
